# tidytemps 0.0.0.9000

* Adds sample dataset, `electricity`.

* Adds `assert_tidytemps()` function to assert that a data frame can be used
  with this package.

* Adds a `NEWS.md` file to track changes to the package.



